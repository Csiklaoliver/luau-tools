-- Shared config accessible from both server and client
local GameConfig = {}

GameConfig.WALK_SPEED = 16
GameConfig.JUMP_POWER = 50
GameConfig.MAX_HEALTH = 100

GameConfig.RESPAWN_TIME = 5 -- seconds

return GameConfig
